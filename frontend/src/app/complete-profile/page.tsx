'use client';

import { CompleteProfile } from '@/views/CompleteProfile';

/**
 * Página de completar perfil (OAuth users que precisam cadastrar telefone)
 */
export default function CompleteProfilePage() {
  return <CompleteProfile />;
}
