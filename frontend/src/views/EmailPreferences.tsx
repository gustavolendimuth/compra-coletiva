/**
 * Email Preferences Page
 * Página standalone para gerenciar preferências de email (acessível via link de emails)
 */

import { EmailPreferencesSection } from './profile/EmailPreferencesSection';

export function EmailPreferences() {
  return <EmailPreferencesSection standalone />;
}
